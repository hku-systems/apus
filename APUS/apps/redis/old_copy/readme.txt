Redis has its own redis-benchmark to test round trip time of write operation.

Install docker and set up test environment
> sudo ./mk

Run redis-master in docker
> sudo ./run

There is a file called result.txt in the current directory
> vi `pwd`/result.txt
